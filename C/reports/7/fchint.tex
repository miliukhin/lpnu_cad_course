\documentclass[a4paper,14pt]{extreport}
\usepackage{tikz}
\usepackage[english,ukrainian]{babel}
\usepackage[T2A]{fontenc}
\usetikzlibrary{calc,positioning,shapes.geometric,shapes.symbols,shapes.misc}
\pagestyle{empty}
\begin{document}
\tikzset{
    start-end/.style={
        draw,
        rounded rectangle,
%        rounded corners,
    },
    input/.style={ % requires library shapes.geometric
        draw,
        trapezium,
        trapezium left angle=60,
        trapezium right angle=120,
    },
    operation/.style={
        draw,
        rectangle
    },
    loop/.style={ % requires library shapes.misc
        draw,
        chamfered rectangle,
        chamfered rectangle xsep=2cm
    },
    decision/.style={ % requires library shapes.geometric
        draw,
        diamond,
    %text width=6cm,
        aspect=#1
    },
    decision/.default=1,
    print/.style={ % requires library shapes.symbols
        draw,
        tape,
        tape bend top=none
    },
}

\begin{tikzpicture}
\node[start-end] (start) {Початок};

\node[below of=start,input](inp){крок (N)};

	\node[below=3mm of inp,decision](intloop){$x\leq$ \pi?}; %\geq

	\node[below=5mm of intloop](bint){так};
	\node[below=3mm of bint,operation](int){$intg=intg+N\cdot y;$};
	\node[below=3mm of int,operation](x1){$x=x+N$};

	\node[right=5mm of int](rint){$\cdot$};
	\node[left=13mm of x1](lint){ні};
\node[below of=x1,input](intout){вивід значення $intg$};
\node[below of=intout,start-end] (end) {Кінець};

	\draw [thin] (start) -- (inp);
	\draw [thin] (intloop) -- (inp);
	\draw [thin] (intloop) -- (bint);
	\draw [thin] (int) -- (bint);
	\draw [thin] (int) -- (x1);
	\draw [-latex] (int) --  (rint) |- (intloop);
	\draw [thin] (intloop) [-latex] -| (lint);
%	\draw [thin] (intout) -- (int);
	\draw [thin] (intout) -| (lint);
	\draw [thin] (intout) -- (end);

\end{tikzpicture}

\end{document}
